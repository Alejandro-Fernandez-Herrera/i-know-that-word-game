package fileManager;



public class User {
/**
 * actions performed by the users, they may log in, create a new account or exit the program
 */

    public User {
        FileManager fileManager = new FileManager();
        userList = fileManager.reader("users.txt");
        levelList = fileManager.reader("levels.txt");
        position = 0;
        user = "";
        level = 0;

    }






}